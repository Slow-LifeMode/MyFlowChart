<UserControl x:Class="MyFlowChart.Controls.OperatorPaletteControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="220">
    <Border Background="#FFFFFF"
            BorderBrush="#DDE5E3"
            BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Background="#F4F8F7"
                    BorderBrush="#DDE5E3"
                    BorderThickness="0,0,0,1"
                    CornerRadius="6,6,0,0">
                <TextBlock Margin="14,0"
                           VerticalAlignment="Center"
                           FontFamily="Microsoft YaHei"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#263432"
                           Text="算子模块" />
            </Border>

            <ListBox Grid.Row="1"
                     ItemsSource="{Binding Operators, ElementName=Root}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="8"
                     PreviewMouseLeftButtonDown="OperatorList_PreviewMouseLeftButtonDown"
                     PreviewMouseMove="OperatorList_PreviewMouseMove">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Margin" Value="0,0,0,8" />
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Height="42"
                                Background="#F8FCFB"
                                BorderBrush="#CFECE2"
                                BorderThickness="1"
                                CornerRadius="6"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Background="#38D99A"
                                        CornerRadius="6,0,0,6" />
                                <TextBlock Grid.Column="1"
                                           Margin="10,0,8,0"
                                           VerticalAlignment="Center"
                                           FontFamily="Microsoft YaHei"
                                           FontSize="13"
                                           Foreground="#263432"
                                           Text="{Binding Name}"
                                           TextTrimming="CharacterEllipsis" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
